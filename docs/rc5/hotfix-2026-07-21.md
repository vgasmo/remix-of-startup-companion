# RC5 Hotfix — 2026-07-21

Status: **NO-GO for RC5.** This turn shipped only the highest-severity in-production hotfixes plus a kill-switch. Batches A (rest), B, C, D, E, F, G remain incomplete and the previous `final-close-out.md` overstated coverage.

## Applied migration state (before / after this turn)

Applied `2026072*` migrations queried from `supabase_migrations.schema_migrations`:

```
20260720155324  20260720155355  20260720155643
20260721070803  20260721071131  20260721071619  20260721072306
20260721084054  20260721112523
20260721124424  (A1/A2/A3 initial — defective RPC)
20260721125042  20260721125336
20260721125838  (Monthly Founder Pulse tables + cron)
20260721144756  20260721145814
+ this turn: log_completed_session_atomic corrective replacement (in-place function rewrite)
```

## Phase 0 evidence (executed against production DB via read_query)

- `app_role` enum values (real): `admin, consultor, mentor_externo, founder, team_member, backoffice`. **`staff` does NOT exist** — every code path casting `'staff'::app_role` fails at execution.
- `public.tool_usage_events` real columns: `id, tool, entity_type, entity_id, workspace_id, user_id, session_id, occurred_at, metadata`. Previous RPC wrote `tool_key`, `event_type` — undefined columns.
- `public.sessions` has `completed_at`, `command_id`, `actual_duration_minutes`, `source`, `outlook_sync_status` (all present; previous RPC failed to set `completed_at`).
- `startup_contracts` columns `counter_signer_email`, `counter_signer_name`, `counter_signer_status` all exist; the public onboarding SELECT did NOT include them, so `hasCounterSigner` always read undefined and bilateral contracts risked silent unilateral activation.

## What actually shipped this turn (hotfix scope only)

1. **A1 partial — `log_completed_session_atomic` corrective RPC** (`log_completed_session_atomic` replaced in-place; forward-only, no data touched):
   - Removed the `'staff'::app_role` cast (which does not exist) and now checks `admin` and `backoffice` roles plus assigned consultor, active workspace member, and mentor-of-workspace.
   - Writes `tool_usage_events` to the real column set (`tool, entity_type, entity_id, session_id, metadata`).
   - Sets `sessions.completed_at = p_occurred_at`.
   - Preserves idempotency short-circuit on `command_id`.
   - `REVOKE`d from `PUBLIC`; `GRANT EXECUTE` to `authenticated` only.
   - **Still open / NOT PROVEN**: no pgTAP test, no fingerprinted command binding to actor+payload, no full attendee-authorization matrix, no participant-role validation beyond client input. See "Residual A1 gaps" below.

2. **B1 partial — bilateral signing fail-visible**: `public-contract-onboarding` token SELECT now includes `counter_signer_email, counter_signer_name, counter_signer_status`. This alone does not make signing atomic — B1/B2 remain **NOT PROVEN** (no locked command, no atomic RPC caller, no explicit consent enforcement).

3. **Kill switch for Monthly Founder Pulse**: new `founder_monthly_pulse` feature flag (default OFF). `FounderPulseCard` on the founder dashboard is now gated on this flag; the DB tables, RPC, and cron remain in place but the questionnaire is invisible until the flag is enabled per-workspace or globally. The scheduled cron `open_monthly_founder_pulse_cycles` is intentionally **not** disabled here because opening a cycle row with no visible UI is safe and reversible; disabling requires an ops action.

## What did NOT ship (honest list — all NOT PROVEN)

- Full A1 authorization matrix + attendee-membership validation, command-fingerprint binding, mentor active-connection enforcement in insert path.
- pgTAP for A1/A2/A3, UI E2E for consultant/founder/mentor/unauthorized matrix.
- B1 atomic public signature RPC caller wiring, explicit consent enforcement, document-hash evidence, work-queue-schema validation.
- B2 provider-safe DocuSign atomic claim + ambiguous-state reconciliation.
- C1 first-contact `AT TIME ZONE 'Europe/Lisbon'`, worker-outbox side effects, concurrency-safe rate limit.
- C2 CRM import uploader-ownership + partial batch state.
- C3 peer-view caller migration; base `profiles` peer policy still present.
- C4 notification-outbox worker, lease-owner enforcement.
- D1–D3 real Monthly Founder Pulse (per-founder email delivery via assigned consultant, hashed single-use tokens, PT/EN template, reminder cadence, DPO sign-off).
- E1 automation registry parity, E2 programme atomic publish, E3 financial atomic caller wiring, E4 RLS argument-order sweep, E5 clickability + autosave E2E.
- F1 restricted supabase import ban, 66 i18n keys, Bun 1.2.0 pin, Deno lint per function, fresh replay + forward apply, `.env` export guard, persona E2E at 320/375/768/1440.

## Contradicting `final-close-out.md`

That document claims Batches A–E are shipped. This ledger supersedes it. Concrete refutations:

- A1 RPC was live with two schema errors and a nonexistent enum cast; it could not have completed a session — the file `src/test/log-completed-session.test.ts` referenced there mocks the RPC and does not exercise the real function.
- D (Monthly Founder Pulse) is an unflagged in-app questionnaire, not the requested consultant-sent email flow; DPO sign-off is not linked.
- C2 outbox has schema and RPCs but no worker/caller migration.

`final-close-out.md` should be treated as aspirational; use this ledger as the current truth of Batch A/B state until each item has a linked failing repro + passing behavior test + implementation diff.

## Rollback / kill switches

- Monthly Pulse UI: unset feature flag `founder_monthly_pulse` (or leave OFF — that is the default).
- A1 RPC: previous function body is preserved in migration `20260721124424`. To roll back, redeploy that body — but doing so re-introduces the production failure; not recommended.
- B1 SELECT: revert the single change in `supabase/functions/public-contract-onboarding/index.ts` lines 823–836.

## Read-only impact queries (no writes performed)

```sql
-- Sessions logged since the corrective RPC deployed
SELECT count(*) FROM public.sessions WHERE command_id IS NOT NULL AND created_at > now() - interval '1 day';

-- Bilateral contracts that already passed through the broken code path
SELECT id, contract_number, signature_status, counter_signer_status
FROM public.startup_contracts
WHERE counter_signer_email IS NOT NULL
  AND signature_status IN ('signed','partially_signed')
ORDER BY updated_at DESC LIMIT 200;

-- Pulse cycles opened while UI was unflagged
SELECT count(*), min(period_month), max(period_month)
FROM public.founder_pulse_cycles WHERE status = 'open';
```

## Verdict

**NO-GO.** The three items above stop concrete production breakage; the remainder of the RC5 rescue plan remains unshipped and must not be marked complete from source grep or typecheck alone.
