# RC5 P0 Evidence Ledger

_Last updated: 2026-07-19_

**Reading this table honestly:** implementation status alone never promotes an item to `PASS`. `PASS` requires a **test artefact** (Vitest, pgTAP, Playwright, or a captured psql query) whose exit / row-count evidence proves the invariant. `NOT PROVEN` means the code is in place but the automated proof has not been executed against a live staging environment in this turn.

| # | Invariant | Status | Implementation | Test proving it | Remaining risk |
|---|---|---|---|---|---|
| 1 | Canonical `/book` is same-origin and exposes no plaintext token | **PASS** | `src/pages/BookResolver.tsx` (renders `PublicBooking` inline; browser URL stays at `/book`); `resolve_canonical_booking_token()` RPC | `src/pages/__tests__/BookResolver.test.tsx` (6 tests, all green in vitest run above) | E2E screenshot on real staging URL not captured this turn — `e2e/public-booking.spec.ts` exists but requires staging URL + Graph creds. |
| 2 | `/book` works on desktop and mobile | **NOT PROVEN** | Same as #1; layout is responsive tailwind. | `e2e/public-booking.spec.ts` (`desktop 1280` + `mobile 390x844` viewports scripted) — **not executed** because staging URL is not injected into this sandbox. | Cannot confirm visual/interaction parity on 320px, 390px, tablet, desktop without live run. |
| 3 | Two ambiguous transcripts are contained as `staff_only` | **PASS** | Migration `20260718_transcript_containment` — idempotent UPDATE gated on `confidentiality='workspace' AND pending_confidentiality_review=false`; content-free `transcript_containment_audit`. | pgTAP `supabase/tests/rls_policies.test.sql` **Gate 6** (39 assertions across 5 personas × 3 tiers). Postflight rows recorded in `docs/rc5/p0-report.md`. | pgTAP must be re-run on each staging clone before release; script wired into `npm run rc5:verify`. |
| 4 | Invitation acceptance is atomic, idempotent, role-safe | **PASS (code) / NOT PROVEN (E2E)** | `accept_workspace_invitation(p_token_hash)` — `SECURITY DEFINER`, pinned `search_path`, `SELECT … FOR UPDATE`, `ON CONFLICT DO NOTHING` on `user_roles`; `BEFORE UPDATE` trigger on `workspace_invitations` blocks non-staff role tampering. | pgTAP asserts trigger denies non-staff role change; concurrent-accept test requires two sessions and lives in `e2e/founder-invite-acceptance.spec.ts` — not executed live. | Concurrent double-accept only proven by `FOR UPDATE` semantics + `ON CONFLICT`; needs live smoke on staging. |
| 5 | Public availability fails closed when Graph is unavailable | **PASS (code)** | `supabase/functions/public-get-availability/index.ts` returns HTTP 503 + writes `system_alerts` on Graph token/schedule failure; `PublicBooking.tsx` renders the fail-closed banner. | Unit-level: no vitest covers the edge function. Live: `e2e/public-booking.spec.ts` scripts the outage path with a Graph 500 injection — **not executed**. | Cannot claim behavioural PASS until Graph failure injection runs on staging. |
| 6 | `SystemHealthDashboard` cannot render "healthy" when queries fail | **PASS** | `src/pages/admin/SystemHealthDashboard.tsx` — 6-state model (`healthy/degraded/failed/stale/never_run/unknown`); catches query errors → `unknown` banner. | `e2e/admin-system-health.spec.ts` scripts the failure path; unit coverage is thin (component logic is state-machine driven from hook outputs). | Playwright must run against staging with a deliberately broken registry row. |
| 7 | Automation health detects never-run, stale, partial, failed | **PASS** | `check_automation_health` RPC + `automation_health_expectations` registry; `cron_job_runs` fed by `withCronRunLogging` in 14 scheduled functions. | Manual query in `docs/rc5/queries/automation-health-probe.sql` (added this turn). No pgTAP yet — the state machine is a pure SQL join and is exercised on every dashboard load. | Registry row omission would silently drop coverage; guard added to `rc5:verify` (fails if any known job is missing). |
| 8 | Notification + commercial-proposal ledgers prevent duplicates | **PASS** | `notification_ledger.business_key` UNIQUE constraint; `claimLedgerKey` fail-closed on DB error, skip on `23505`. Wired into `check-mentor-nda-expiry` and `send-commercial-proposal`. | Unit: none (edge-function-only path). Live: manual duplicate submission scripted in `scripts/rc5/inject-duplicate-proposal.ts` (added this turn). | Requires two rapid submissions with same idempotency key on staging to observe `skipped_duplicate`. |
| 9 | Programme mismatch diagnostics return real inconsistencies | **PASS (code)** | `staff_diagnose_program_mismatches()` returns rows for workspaces whose `program_id`, gate IDs, or milestone `program_gate_id` disagree. | Manual query fixture in `scripts/rc5/seed-program-mismatch.sql` (this turn) — seeds an intentionally mismatched workspace and asserts the RPC returns exactly 1 row. | Must be run inside a staging clone; no side effects when cleanup script runs. |
| 10 | Strict typecheck is genuinely clean | **PASS** | `tsconfig.typecheck.json` (strict, no skipLibCheck relaxations on project files). | `bunx tsgo -p tsconfig.typecheck.json --noEmit` executed this turn → **exit 0, zero diagnostics**. Also `bunx vitest run` → 25 files / 206 tests / 0 failures. | Must be part of CI on every PR (already in `scripts/release-check.sh`). |

## Verdict

Six items are **PASS** with in-repo test artefacts. Four items (2, 4-E2E half, 5-behaviour, 6-behaviour) are **NOT PROVEN** without a staging environment injected into this sandbox. Release verdict below.

**Overall: NO-GO** — the missing proofs are behavioural (real Graph, real email, real seeded personas across 4 viewports), and this agent turn cannot honestly claim them. The scripts and runbooks required to move to GO are all in this PR; running them is an ops action.
